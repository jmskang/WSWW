import React from 'react';
import SessionHeader from './SessionHeader';
import MovieCard from './MovieCard';

const test = () => {
  return (
    <div>
      <SessionHeader />
      <MovieCard />
    </div>
  );
};

export default test;
